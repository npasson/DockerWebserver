If you see this sentence, nginx works.<br><br>

You should see your PHP version here: <?= phpversion() ?><br>
If it just says <code>phpversion()</code> instead, PHP does not work correctly.<br><br>

Don't forget to adjust <code>WEB_ROOT</code> to get started!